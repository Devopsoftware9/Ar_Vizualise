using UnityEngine;
using UnityEngine.UI;

public class blurPart : MonoBehaviour
{
    public Button b;

    void Start()
    {
        b.onClick.AddListener(blur);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    void blur()
    {
        
    }
}
